# AI-SkinScope 프로젝트 기획서

**팀미션 3-3) AI 네이티브 Final Project 자율 주제**
작성자: 서경환 (GN_마리너) | 과정: 2026 경남 코디세이(Gyeongnam Codyssey) AI 네이티브 과정
작성일: 2026-08-24

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 서비스명(가제) | AI-SkinScope — AI 피부 상태 스크리닝 & 코칭 서비스 |
| 한 줄 정의 | 스마트폰 카메라로 피부 트러블 부위를 촬영하면 AI가 상태를 분석해 건강 점수와 참고용 가이드를 제공하고, 시간에 따른 변화를 추적해주는 개인 피부 코칭 서비스 |
| 플랫폼 | 모바일 친화 웹 (PWA, 폰 카메라 즉시 촬영) |
| 대상 사용자 | 일반 대중 (실사용자 5명 이상 테스트) |

---

## 2. 문제 정의

| 구분 | 내용 |
|---|---|
| 현재 문제 | 피부에 트러블(여드름, 붉은기, 각질, 건조 등)이 생겼을 때 "병원 갈 정도인지", "그냥 넘어가도 되는지" 스스로 판단하기 어렵다 |
| 기존 대안의 한계 | 인터넷 검색은 정보가 파편적이고 과장되어 불안만 키움. 병원은 매번 가기엔 시간·비용 부담. 기존 앱들은 진단 없이 정보 나열만 함 |
| 핵심 가설 | 사진 한 장으로 즉시 참고용 스크리닝 결과와 관리 가이드를 받고, 시간 흐름에 따른 변화를 눈으로 확인할 수 있다면 병원 방문 시점을 더 합리적으로 판단할 수 있다 |

### 타겟 사용자 (페르소나)

| 페르소나 | 설명 | 니즈 |
|---|---|---|
| 김민지 (26세, 직장인) | 스트레스성 트러블이 반복됨. 피부과는 예약이 번거로워 미루는 편 | 지금 상태가 그냥 뾰루지인지 심각한 건지 빨리 알고 싶다 |
| 박준호 (32세, 직장인) | 최근 이유 없이 건조함·각질이 심해짐 | 내 피부가 나아지고 있는지 나빠지고 있는지 기록하고 싶다 |
| 이서연 (20세, 대학생) | 화장품을 자주 바꿔가며 씀 | 제품 바꾼 뒤 피부가 실제로 좋아졌는지 객관적으로 확인하고 싶다 |

---

## 3. AI 활용 방식

| 기술 요소 | 역할 | 상세 |
|---|---|---|
| 멀티모달 AI (Gemini Vision) | 이미지 분석 | 피부 사진 → 붉은기/각질/여드름/색소침착 등 특징 추출, 구조화된 응답 생성 |
| AI Agent | 의사결정 및 도구 호출 | 분석 결과 기반 ① 건강 점수 산출 ② 유사 증상 카테고리 안내(진단 아님) ③ 심각도 높으면 카카오맵 병원 검색 도구 호출 ④ 관리 루틴 추천 |
| Long-term Memory | 개인화 및 추적 | 사용자별 촬영 이력 저장 → 시간 경과에 따른 점수 변화 그래프, "지난주 대비 개선/악화" 코칭 메시지 생성 |

필수 기술 요소 3개(AI Agent, 멀티모달 AI, Long-term Memory)를 모두 포함하여 미션 요구사항(2개 이상)을 상회 충족한다.

---

## 4. 기술적 접근 방식 및 선정 이유

```
[Frontend: 모바일 친화 웹 / PWA]
   폰 카메라로 사진 촬영 → 업로드
        ↓
[Backend: FastAPI]
   이미지 수신 → Gemini Vision API 호출 (피부 특징 추출)
        ↓
   Agent 로직: 점수 산출 + 카테고리 매핑 + 도구 호출 판단
        ↓
   DB 저장 (사용자별 이력) → Long-term Memory 조회 시 활용
        ↓
[배포: Render 또는 Railway]
   외부 접근 가능한 URL로 서비스 제공
```

| 기술 | 선정 이유 |
|---|---|
| Gemini (Google AI Studio) | 팀이 이전 프로젝트(AI 브랜드 아이덴티티 생성기)에서 이미지 관련 API 활용 경험 보유, 무료/저비용 티어로 프로토타입 개발에 적합 |
| FastAPI | 비동기 처리에 강해 AI API 호출 대기시간 관리에 유리, Swagger 자동 문서화 제공 |
| Render / Railway | 무료 티어로 빠른 배포 가능, GitHub 연동 CI/CD 지원 |
| SQLite → PostgreSQL 전환 가능 | 프로토타이핑 단계에서는 SQLite로 빠르게 시작, 사용자 증가 시 확장 가능한 스키마 설계 |
| 카카오맵 API | 국내 서비스 특성상 병원 검색 정확도가 높고, 무료 사용량 내에서 프로토타입 요건 충족 |

---

## 5. 기능 요구사항 (FR)

| ID | 기능명 | 설명 | 우선순위 |
|---|---|---|---|
| FR-01 | 사진 촬영/업로드 | 폰 카메라로 피부 부위 즉시 촬영 또는 갤러리 업로드 | Must |
| FR-02 | 촬영 전 동의 절차 | 개인정보(민감정보) 수집 동의 체크박스, 저장/삭제 정책 안내 | Must |
| FR-03 | AI 이미지 분석 | Gemini Vision으로 붉은기·각질·여드름·색소침착 등 특징 추출 | Must |
| FR-04 | 피부 건강 점수 산출 | 분석 결과 기반 0~100점 수치화 (AI Hub 한국인 피부상태 측정 데이터의 평가 항목 체계 참고) | Must |
| FR-05 | 유사 증상 카테고리 안내 | "진단 아님" 고지 포함, 5종 패턴(건선/아토피/여드름/지루피부염/주사) 중 유사도 안내 | Must |
| FR-06 | 전문의 상담 권장 트리거 | 심각도 임계값 초과 시 "피부과 방문 권장" 메시지 + 카카오맵 병원 검색 도구 호출 | Must |
| FR-07 | 관리 루틴 코칭 메시지 | 분석 결과 기반 생활 습관/관리 팁 생성 (AI 생성 콘텐츠 명시) | Must |
| FR-08 | 이력 저장 (Long-term Memory) | 사용자별 촬영 기록·점수 DB 저장 | Must |
| FR-09 | 변화 추적 리포트 | 이전 기록 대비 점수 변화 그래프 + "지난주 대비" 코칭 메시지 | Must |
| FR-10 | 사용자 피드백 수집 | 분석 결과 만족도/의견 입력 폼 | Must |
| FR-11 | 결과 히스토리 조회 | 과거 촬영 이력 리스트/상세 조회 | Should |
| FR-12 | 데이터 삭제 요청 | 사용자가 본인 데이터 삭제 가능 | Should |

## 6. 비기능 요구사항 (NFR)

| ID | 항목 | 요구사항 |
|---|---|---|
| NFR-01 | 응답 속도 | 사진 업로드 → 분석 결과까지 10초 이내 |
| NFR-02 | 개인정보 보호 | 사진·건강정보 암호화 저장, 접근 권한 최소화 |
| NFR-03 | 가용성 | 배포 URL 상시 접근 가능 (무료 티어 콜드스타트 감안) |
| NFR-04 | 사용성 | 모바일 브라우저에서 카메라 접근 3탭 이내 |
| NFR-05 | 투명성 | 모든 결과 화면에 "AI 생성/진단 아님" 고지 상시 노출 |
| NFR-06 | 확장성 | Long-term Memory용 DB는 사용자 수 증가 대비 정규화된 스키마로 설계 |

## 7. 시스템 아키텍처

```
┌─────────────────────────┐
│   Frontend (PWA)         │
│   - 카메라 촬영 UI        │
│   - 결과/대시보드 화면    │
└───────────┬──────────────┘
            │ HTTPS (사진 업로드)
            ▼
┌─────────────────────────┐
│   Backend (FastAPI)      │
│   ┌─────────────────┐   │
│   │ Vision Module     │──┼──▶ Gemini Vision API
│   │ (FR-03)          │   │
│   └────────┬──────────┘   │
│            ▼               │
│   ┌─────────────────┐   │
│   │ Agent Module      │   │
│   │ (점수화/카테고리/  │──┼──▶ 카카오맵 검색 도구
│   │  도구호출 판단)     │   │   (FR-06)
│   │ (FR-04~07)        │   │
│   └────────┬──────────┘   │
│            ▼               │
│   ┌─────────────────┐   │
│   │ Memory Module      │──┼──▶ DB (사용자별 이력)
│   │ (FR-08, 09)       │   │
│   └─────────────────┘   │
└───────────┬──────────────┘
            │
            ▼
      Render / Railway (배포)
```

---

## 8. AI Hub 개방 데이터 활용 계획

미션 진행 과정에서 피부 관련 AI Hub 데이터셋 3종을 검토했으며, 각 데이터셋의 접근 가능 여부와 활용 방식을 아래와 같이 확정한다.

| 데이터셋 | 접근 가능 여부 | 판단 근거 | 활용 방식 |
|---|---|---|---|
| 피부 질환 진단 의료 이미지 (dataSetSn=34105) | ❌ 사용 불가 | 오프라인 안심존 데이터 — IRB 심의 결과 통지서, 소속 증빙 서류, 지정된 물리적 공간 접속이 필요해 학생 개인이 프로젝트 일정 내 충족 불가능. 원본 반출 및 논문 이미지 삽입도 금지됨 | 미사용 |
| 안면부 피부질환 이미지 합성 데이터 (dataSetSn=71863) | ✅ 사용 가능 | 실제 환자가 아닌 AI 합성 데이터로 개인정보 이슈 없음. 다운로드 버튼 직접 노출 확인 | 건선/아토피피부염/여드름/지루피부염/주사 5종 질환의 정의·특징을 Gemini Vision 프롬프트의 few-shot 판단 기준으로 반영. 개발 단계 데모/테스트 이미지로 활용 |
| 한국인 피부상태 측정 데이터 (dataSetSn=71645) | ✅ 사용 가능 | 헬스케어·건강서비스 분류의 개방 데이터. 다운로드/샘플(경량) 데이터 버튼 노출 확인 | 라벨링 필드(모공, 탄력, 수분, 주름, 색소침착 등)를 피부 건강 점수(FR-04) 산출 로직의 항목·가중치 설계 기준으로 참고 |

### 데이터 사용 원칙

- 비상업적 연구·개발 목적(코디세이 캡스톤 프로젝트)으로만 사용
- 결과물(발표자료, 보고서, README)에 AI Hub 데이터셋 출처를 명시
- 원본 이미지 파일을 GitHub 공개 저장소나 배포된 서비스에 그대로 재배포하지 않음. 학습·프롬프트 설계·데모 검증 용도로만 로컬에서 활용
- 실사용자 테스트에 사용되는 사진은 AI Hub 데이터가 아닌, 서비스 이용 동의를 받은 실제 테스터 본인의 사진만 사용

### 8-1. 실제 데이터 스키마 확인 결과

두 데이터셋을 실제로 열람하여 확인한 스키마는 다음과 같다.

**안면부 피부질환 이미지 합성 데이터** — 이미지 1장당 JSON 라벨 1개, `diagnosis_info` 객체 안에 `diagnosis_name`(질환명), `desc`(공식 임상 설명), `bodypart`(발생 부위), `distribution`(침범 범위) 등을 포함. 병변 영역만 마스킹한 `lesion_area` 이미지도 별도 제공.

**한국인 피부상태 측정 데이터** — 이미지 1장당 얼굴 부위(facepart 0~6: 전체/이마/미간/좌우 눈가/좌우 볼)별로 JSON이 분리되어 총 7개 존재하며, 부위별로 `annotations`(전문의 육안 등급, 정수)와 `equipment`(정밀 측정 장비 실측값, 연속값)가 이중으로 기록됨. 별도의 `measurement_data.csv`에는 전체 피험자(n=1,072)의 수분·탄력·주름·색소·모공 실측값이 원자료로 제공됨.

### 8-2. Gemini 프롬프트 설계 방법론

**채택한 방식: 텍스트 기반 근거 주입 (Text-grounded Prompting)**

사용자 사진을 분석할 때마다 원본 데이터셋의 이미지나 실측값을 실시간으로 참조하는 대신, 데이터셋에서 추출한 핵심 지식을 텍스트로 정제하여 Gemini Vision 프롬프트(시스템 프롬프트)에 고정 삽입하는 방식을 채택했다. 구체적으로 두 가지 근거를 프롬프트에 반영한다.

1. **질환 정의 근거**: `안면부 피부질환 이미지 합성 데이터`의 `diagnosis_info.desc` 필드 원문 5종(건선·아토피피부염·여드름·지루피부염·주사)을 그대로 few-shot 판단 기준으로 사용. 임의로 작성한 설명이 아닌 데이터셋 공식 정의를 근거로 삼는다.
2. **점수화 근거(인구통계 anchor)**: `measurement_data.csv` 전체(n=1,072)를 분석하여 산출한 실측 분포(평균·백분위)를 Gemini 프롬프트에 명시한다. 사진만으로는 장비 실측이 불가능하므로, Gemini가 "이 정도면 인구 상위/하위 몇 % 수준"이라는 방식으로 판단하도록 보정 기준점을 제공한다.

| 지표 | 평균 | 하위 5% | 중앙값 | 상위 5% |
|---|---|---|---|---|
| 수분(%) | 60.4 | 43.7 | 60.7 | 76.0 |
| 탄력(Q0) | 49.5~50.9 | 32~34 | 48~50 | 71~73 |
| 주름 거칠기(Ra, 눈가) | 21.7~21.9 | 14.0 | 21.0 | 32~33 |
| 색소반점 개수 | 159 | 57 | 157 | 266 |
| 모공 개수(볼) | 881~932 | 230~241 | 843~902 | 1650~1781 |

**채택 이유**

- 사용자가 촬영한 실제 사진에는 애초에 장비 실측값이나 전문의 등급 라벨이 존재하지 않으므로, Gemini는 시각적 인상에 기반한 판단을 할 수밖에 없다. 텍스트 근거 주입은 이 판단에 "무엇이 평균이고 무엇이 심한 수준인지"에 대한 현실적인 기준점을 제공하면서도 구조가 단순하다.
- API 호출마다 추가 이미지나 벡터 검색 없이 고정 텍스트만 포함되므로 토큰 비용과 응답 지연이 최소화된다.
- 데이터셋의 `annotations` 등급(1~3 등 정수)은 정확한 척도 정의(예: 0~5 등)를 AI Hub 문서에서 확인할 수 없어 그대로 복제할 경우 오히려 자의적인 해석이 될 위험이 있었다. 반면 `equipment`의 연속 실측값은 통계적으로 명확히 정의되므로 이를 기준으로 삼는 것이 더 근거가 탄탄하다.

**검토했으나 채택하지 않은 대안**

| 대안 | 내용 | 기각 사유 |
|---|---|---|
| 원본 등급(annotations) 척도 그대로 복제 | AI Hub 라벨의 1~3 등급 체계를 Gemini 출력 스키마에 그대로 반영 | 척도의 정확한 정의(등급 범위, 각 숫자의 의미)를 문서에서 확인할 수 없어 신뢰도 있는 매핑이 불가능 |
| 멀티모달 few-shot (질환별 대표 이미지 첨부) | 매 요청마다 질환별 샘플 이미지를 프롬프트에 함께 전송 | 정확도 개선 효과 대비 토큰 비용·응답 지연 증가가 크고, 남은 일정 대비 우선순위가 낮음. 여유 시간 확보 시 추가 검토 |
| RAG (이미지 임베딩 유사도 검색) | 사용자 사진을 임베딩하여 원본 데이터셋과 벡터 유사도 비교 후 최유사 사례를 few-shot으로 주입 | 벡터DB 구축 등 엔지니어링 공수가 크고, 원본 이미지가 서버 내부 비교 용도를 넘어 실수로 노출될 경우 AI Hub 재배포 금지 조항 위반 위험이 있음. 5개 필수 기술요소 중 RAG까지 추가하면 차별화되지만 일정상 보류 |
| Gemini 모델 파인튜닝 | 원본 데이터셋으로 Gemini 자체를 추가 학습 | Google AI Studio/Gemini API에서 파인튜닝 자체가 지원 중단되어(2025년 5월) Gemini Enterprise Agent Platform(GCP 유료 인프라)에서만 가능. 캡스톤 프로젝트 범위를 벗어남 |

---

## 9. AI 윤리 준수 사항 (미션 필수 요건)

### 9-1. AI 생성 콘텐츠 명시

- 모든 분석 결과 화면에 다음 문구를 상시 노출한다: **"본 결과는 AI 참고용 스크리닝이며 의료 진단이 아닙니다. 정확한 진단은 피부과 전문의와 상담하세요."**
- 건강 점수, 유사 증상 카테고리, 관리 루틴 코칭 메시지는 모두 AI가 생성한 콘텐츠임을 결과 화면에 별도 표기한다.
- "의심 질환 진단"이 아닌 "스크리닝/참고용 가이드"로 서비스 포지셔닝을 확정한다. 병명을 확정적으로 제시하지 않고, 유사 패턴 안내 + 전문의 상담 권장 문구를 항상 함께 제공한다.

### 9-2. 개인정보 수집 및 관리

- 피부 사진 및 건강 관련 정보는 민감정보로 간주하며, 촬영 전 **명시적 동의 체크박스**(FR-02)를 통해 사용자 동의를 받는다.
- 동의 화면에는 촬영 목적, 저장 기간, 삭제 방법을 명확히 안내한다.
- 사용자는 본인의 데이터를 언제든 삭제 요청할 수 있다 (FR-12).
- 저장되는 사진·건강정보는 암호화하여 보관하고, 접근 권한을 최소화한다 (NFR-02).
- 실사용자 테스트 참여자에게는 별도의 테스트 참여 동의서를 통해 데이터 수집·활용 목적을 사전 고지한다.

### 9-3. 진단 오남용 방지

- 심각도가 높다고 판단되는 경우 반드시 병원 방문을 권장하는 방향으로 Agent 로직을 설계하며, 사용자가 AI 결과만으로 병원 방문을 미루지 않도록 안내한다.
- Gemini Vision의 분석 결과는 확률적·참고적 정보이며 확정적 의학적 사실이 아님을 프롬프트 설계 및 UI 문구 양쪽에서 일관되게 전달한다.

---

## 10. 일정 계획 (6주 기준, 협의 가능)

| 주차 | 작업 |
|---|---|
| 1주차 | 기획서·요구명세서 확정, 개발 환경 구성 |
| 2주차 | 멀티모달 이미지 분석 모듈 구현 |
| 3주차 | AI Agent 로직(점수화·카테고리·도구호출) 구현 |
| 4주차 | Long-term Memory + 프론트엔드(PWA) 구현 |
| 5주차 | 통합·배포, 실사용자 테스트(5명 이상) 진행 |
| 6주차 | 피드백 반영, 발표자료·최종 제출 보고서 작성 |

---

## 11. 팀 구성 및 진행 방식

- 서경환(GN_마리너)이 먼저 솔로로 스캐폴드 및 핵심 기능(Vision/Agent/Memory 모듈)을 구현
- 이후 팀원별 역할을 분담하여 프론트엔드, 테스트, 배포 등을 협업으로 확장
- 미션 요구사항에 따라 최종적으로 팀원 3~5인 구성과 각자의 기여 내역을 README.md 및 최종 보고서에 명시
